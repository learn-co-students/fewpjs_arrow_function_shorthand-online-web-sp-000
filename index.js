// functional expression (fn(){}) called divide
let divide = function(){
  return 2000 / 100;
};

// has an arrow function called square
  // multiplies a number times itself
let square = num => num * num;

// has an arrow function called add
  // takes two parameters and adds them together
let add = (parameter1, parameter2) => parameter1 + parameter2; 