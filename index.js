const divide = function(){
    return 2000/100 // The anonymous function is to the right of the "="
}

const square = x => x ** x // Since it only has one parameter, we do not need '()' with an arrow function

const add = (x, y) => x + y // With multiple paramters, we then must use '()'.


